import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Importation du fichier de style global
import App from './App'; // Importation du composant App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
